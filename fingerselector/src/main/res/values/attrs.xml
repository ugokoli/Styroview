<?xml version="1.0" encoding="utf-8"?>

<resources>
    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->

    <declare-styleable name="FingerSelector">
        <attr name="defaultHand" format="enum">
            <enum name="right" value="0"/>
            <enum name="left" value="1"/>
        </attr>
        <attr name="defaultFinger" format="enum">
            <enum name="any" value="0"/>
            <enum name="thumb" value="1"/>
            <enum name="index" value="2"/>
            <enum name="middle" value="3"/>
            <enum name="ring" value="4"/>
            <enum name="pinky" value="5"/>
        </attr>
    </declare-styleable>
</resources>