<resources>
    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->

    <declare-styleable name="PinCode">
        <attr name="fieldShape" format="enum">
            <enum name="line" value="1"/>
            <enum name="rectangle" value="2"/>
            <enum name="circle" value="3"/>
        </attr>
        <attr name="codeColor" format="string" />
        <attr name="defaultColor" format="string" />
        <attr name="selectedColor" format="string" />
        <attr name="filledColor" format="string" />
        <attr name="maskCode" format="boolean" />
    </declare-styleable>
</resources>
